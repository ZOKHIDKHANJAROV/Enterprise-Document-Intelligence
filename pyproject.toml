[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-document-intelligence"
version = "0.1.0"
description = "Enterprise document intelligence research project with QLoRA and RAG"
requires-python = ">=3.10"
